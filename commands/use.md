---
name: use
description: Switch taste to a named preset (e.g. /taste use carmack)
arg-hint: <preset>
---

Run: `node ${CLAUDE_PLUGIN_ROOT}/dist/cli.js use $ARGUMENTS`

After the command finishes, read `.taste/profile.md` and confirm the preset is active. Suggest that the next edit will be judged against it.
