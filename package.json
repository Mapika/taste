{
  "name": "taste",
  "version": "0.1.0",
  "description": "AI taste judge for Claude Code. Your code. Your taste.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "taste": "./dist/cli.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "eval": "node dist/eval/harness.js",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
