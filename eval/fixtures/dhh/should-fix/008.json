{
  "id": "008",
  "description": "PaymentProcessingService — business logic in a service object that belongs in a model or job",
  "input": "class PaymentProcessingService\n  def initialize(order, payment_method)\n    @order = order\n    @payment_method = payment_method\n  end\n\n  def process\n    charge = Stripe::Charge.create(\n      amount: @order.total_cents,\n      currency: 'usd',\n      source: @payment_method.stripe_token\n    )\n    @order.update!(charge_id: charge.id, paid: true)\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["Service", "model", "job"]
  }
}
