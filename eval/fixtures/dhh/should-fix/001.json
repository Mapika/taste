{
  "id": "001",
  "description": "Service-suffix class wraps model logic that belongs in the model",
  "input": "class PostCreationService\n  def initialize(params)\n    @params = params\n  end\n\n  def call\n    post = Post.new(@params)\n    post.save!\n    post\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["Service", "model"]
  }
}
