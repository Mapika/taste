{
  "id": "016",
  "description": "explicit nil checks instead of ActiveSupport blank?/present? idiom",
  "input": "def display_name\n  if !@user.name.nil? && @user.name != '' && !@user.name.strip.empty?\n    @user.name\n  elsif !@user.username.nil? && @user.username != ''\n    @user.username\n  else\n    'Anonymous'\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.75,
    "violationContains": ["present?", "blank?", "ActiveSupport"]
  }
}
