{
  "id": "002",
  "description": "UserManagerService suffix — double violation: Manager and Service both signal Java-brain ceremony",
  "input": "class UserManagerService\n  def self.authenticate(user, password)\n    user.password_digest == BCrypt::Password.new(password)\n  end\n\n  def self.deactivate(user)\n    user.update!(active: false)\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.9,
    "violationContains": ["Service", "model"]
  }
}
