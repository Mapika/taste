{
  "id": "020",
  "description": "explicit require + utility-library import pattern when Rails already provides the functionality",
  "input": "require 'lodash'\nrequire 'underscore'\n\nclass ApplicationHelper\n  def truncate_title(title)\n    Lodash.truncate(title, length: 60)\n  end\n\n  def humanize_list(items)\n    Underscore.compact(items).join(', ')\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["ActiveSupport", "stdlib", "truncate"]
  }
}
