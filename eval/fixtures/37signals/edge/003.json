{
  "id": "003",
  "description": "a TODO that is a genuine note to the reader (not an unfinished task) — debatable",
  "input": "def incinerate!\n  ActiveRecord::Base.transaction do\n    # TODO(team): revisit once GDPR data-export requirement lands in Q3\n    # For now, incineration is permanent and irreversible.\n    attachments.each(&:purge)\n    destroy!\n  end\nend",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.6,
    "violationContains": ["TODO", "tracker"]
  }
}
