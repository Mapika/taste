{
  "id": "014",
  "description": "useEffect without cleanup for a subscription — missing return teardown",
  "input": "function NotificationBell({ userId }) {\n  const [count, setCount] = useState(0);\n\n  useEffect(() => {\n    const unsubscribe = notificationService.subscribe(userId, (notifications) => {\n      setCount(notifications.filter(n => !n.read).length);\n    });\n    // forgot to return unsubscribe\n  }, [userId]);\n\n  return <button>Notifications ({count})</button>;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.8,
    "violationContains": ["cleanup", "return"]
  }
}
