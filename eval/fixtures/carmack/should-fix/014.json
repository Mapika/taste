{
  "id": "014",
  "description": "imports: unnecessary utility library import for operations that are one-liners",
  "input": "import { clamp, lerp, degToRad, radToDeg, isPowerOfTwo } from 'lodash-math';\nimport { capitalize, truncate, padStart } from 'lodash/string';\nimport { chunk, flatten, uniq } from 'lodash/array';\n\nfunction buildFrameList(frames: number[]): number[] {\n  return uniq(flatten(chunk(frames, 4)));\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["utility", "import"]
  }
}
