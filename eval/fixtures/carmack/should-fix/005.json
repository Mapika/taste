{
  "id": "005",
  "description": "naming: utility math wrapped in a namespace class with static methods instead of plain functions",
  "input": "export class VectorMath {\n  static dot(a: number[], b: number[]): number {\n    return a[0]*b[0] + a[1]*b[1] + a[2]*b[2];\n  }\n\n  static length(v: number[]): number {\n    return Math.sqrt(v[0]*v[0] + v[1]*v[1] + v[2]*v[2]);\n  }\n\n  static normalize(v: number[]): number[] {\n    const len = VectorMath.length(v);\n    return [v[0]/len, v[1]/len, v[2]/len];\n  }\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["class", "static"]
  }
}
