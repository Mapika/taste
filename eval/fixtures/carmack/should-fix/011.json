{
  "id": "011",
  "description": "error handling: async error swallowed in .catch() instead of crashing",
  "input": "async function readLevel(path: string): Promise<Level | null> {\n  return fs.readFile(path)\n    .then(data => parseLevel(data))\n    .catch(err => {\n      console.error('Failed to read level:', err);\n      return null;\n    });\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["swallowed", "catch"]
  }
}
