{
  "id": "009",
  "description": "optional chaining and nullish coalescing — modern syntax that hides a potential crash Carmack would prefer to surface",
  "input": "function getActorSpeed(actor: Actor): number {\n  return actor?.info?.speed ?? 0;\n}\n\nfunction getSoundTarget(sec: Sector): MapObject | null {\n  return sec?.soundTarget ?? null;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.3,
    "violationContains": ["optional chaining", "null"]
  }
}
