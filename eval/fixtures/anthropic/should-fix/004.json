{
  "id": "004",
  "description": "single-letter variable names and acronym soup in function signature",
  "input": "export function procReq(r: RO, cb: CB, cfg: Cfg): Promise<Res> {\n  const h = cfg.hdrs ?? {};\n  const b = JSON.stringify(r.body);\n  return fetch(r.url, { method: r.m, headers: h, body: b }).then(cb);\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.75,
    "violationContains": ["abbreviat", "naming"]
  }
}
