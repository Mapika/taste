{
  "id": "012",
  "description": "default export for a utility function — anthropic uses named exports",
  "input": "function sleep(ms: number): Promise<void> {\n  return new Promise((resolve) => setTimeout(resolve, ms));\n}\n\nexport default sleep;",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["default export", "named"]
  }
}
