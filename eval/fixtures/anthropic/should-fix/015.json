{
  "id": "015",
  "description": "overly verbose JSDoc that restates the obvious — comments explain what the code already says",
  "input": "/**\n * Returns the length of a string.\n * This function takes a string as input.\n * It uses the built-in length property.\n * @param str - The string whose length to return\n * @returns The number of characters in the string\n */\nexport function getStringLength(str: string): number {\n  return str.length;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.65,
    "violationContains": ["comment", "verbose"]
  }
}
