{
  "id": "007",
  "description": "catch (e: any) with console.error — violates both unknown-typed error and banned token rules",
  "input": "async function fetchData(url: string): Promise<Response> {\n  try {\n    const response = await fetch(url);\n    if (!response.ok) throw new Error(`HTTP error: ${response.status}`);\n    return response;\n  } catch (e: any) {\n    console.error('Request failed:', e);\n    throw e;\n  }\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["any", "unknown"]
  }
}
