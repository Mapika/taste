{
  "id": "008",
  "description": "throwing raw string instead of typed Error subclass",
  "input": "function validateApiKey(key: string | undefined): string {\n  if (!key || key.trim() === '') {\n    throw 'API key is required and cannot be empty';\n  }\n  return key.trim();\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.75,
    "violationContains": ["string", "Error"]
  }
}
