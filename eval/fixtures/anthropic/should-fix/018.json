{
  "id": "018",
  "description": "catch (e: any) — hard rule violation, error parameter must be typed unknown",
  "input": "export async function withRetry<T>(fn: () => Promise<T>, retries = 3): Promise<T> {\n  let lastError: Error;\n  for (let i = 0; i < retries; i++) {\n    try {\n      return await fn();\n    } catch (e: any) {\n      lastError = e;\n      await sleep(1000 * (i + 1));\n    }\n  }\n  throw lastError!;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["any", "unknown"]
  }
}
