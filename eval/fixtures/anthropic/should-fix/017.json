{
  "id": "017",
  "description": "enum instead of string-union type — hard rule violation",
  "input": "export enum LogLevel {\n  Off = 'off',\n  Error = 'error',\n  Warn = 'warn',\n  Info = 'info',\n  Debug = 'debug',\n}\n\nexport enum HttpMethod {\n  Get = 'GET',\n  Post = 'POST',\n  Put = 'PUT',\n  Delete = 'DELETE',\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["enum", "string"]
  }
}
