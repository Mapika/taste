{
  "id": "010",
  "description": "section divider-style comment in a test file — could be acceptable in non-production context",
  "input": "// ---- setup ----\nconst client = new Anthropic({ apiKey: 'test-key' });\n\n// ---- assertions ----\nconst message = await client.messages.create({\n  model: 'claude-opus-4-6',\n  max_tokens: 100,\n  messages: [{ role: 'user', content: 'Hello' }],\n});\nconsole.assert(message.role === 'assistant');",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.3,
    "violationContains": ["comment", "divider"]
  }
}
