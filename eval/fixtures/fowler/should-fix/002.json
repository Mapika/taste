{
  "id": "002",
  "description": "foo/bar/baz placeholder names — banned intention-less identifiers",
  "input": "function processData(foo, bar) {\n    const baz = foo.items.filter(x => x.active);\n    return baz.map(x => bar.transform(x));\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.85,
    "violationContains": ["foo", "bar", "baz"]
  }
}
