{
  "id": "012",
  "description": "abbreviating global function names (cntusr style)",
  "input": "int cntActUsr(void);\nint fndFreeSlot(struct pool *p);\nvoid freeAllRsrc(struct device *dev);\nint chkPerm(struct inode *ino, int mask);",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["abbreviation", "naming", "global"]
  }
}
