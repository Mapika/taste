{
  "id": "002",
  "description": "CamelCase function and parameter names",
  "input": "int processUserRequest(RequestData *requestData, ResponseBuffer *responseBuffer)\n{\n\tint retVal = 0;\n\n\tif (!requestData)\n\t\treturn -EINVAL;\n\tretVal = handleRequest(requestData);\n\treturn retVal;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["CamelCase", "snake_case"]
  }
}
