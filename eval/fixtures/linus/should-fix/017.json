{
  "id": "017",
  "description": "macro that looks like a function but affects control flow",
  "input": "#define CHECK_ERR(err) \\\n\tif ((err) < 0) return (err)\n\nint setup_device(struct device *dev)\n{\n\tint err;\n\n\tCHECK_ERR(device_lock(dev));\n\tCHECK_ERR(device_init(dev));\n\treturn 0;\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.7,
    "violationContains": ["macro", "control flow", "return"]
  }
}
