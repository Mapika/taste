{
  "id": "006",
  "description": "redundant var declaration with type and value — use := instead",
  "input": "func processItems(items []string) error {\n\tvar count int = 0\n\tvar result string = \"\"\n\tvar enabled bool = true\n\n\tfor _, item := range items {\n\t\tif enabled {\n\t\t\tresult = result + item\n\t\t\tcount = count + 1\n\t\t}\n\t}\n\treturn nil\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.8,
    "violationContains": ["var", ":="]
  }
}
