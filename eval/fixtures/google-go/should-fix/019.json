{
  "id": "019",
  "description": "context.Context parameter not first — violates ordering convention",
  "input": "// FetchUser retrieves a user by ID.\nfunc FetchUser(id int, ctx context.Context, db *sql.DB) (*User, error) {\n\trow := db.QueryRowContext(ctx, \"SELECT id, name FROM users WHERE id = $1\", id)\n\tvar u User\n\tif err := row.Scan(&u.ID, &u.Name); err != nil {\n\t\treturn nil, fmt.Errorf(\"FetchUser %d: %w\", id, err)\n\t}\n\treturn &u, nil\n}",
  "expect": {
    "verdict": "fix",
    "minConfidence": 0.8,
    "violationContains": ["context", "first"]
  }
}
